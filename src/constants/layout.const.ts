import type { WindowHandle } from '#models';

export const WINDOW_MIN_WIDTH = 200;
export const WINDOW_MIN_HEIGHT = 150;
export const MINIMIZED_WINDOW_WIDTH = 200;
export const WINDOW_HANDLES: WindowHandle[] = ['n', 's', 'e', 'w', 'ne', 'nw', 'se', 'sw'];
