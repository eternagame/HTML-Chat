diff --git a/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-bjDtDTaP.mjs b/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-bjDtDTaP.mjs
index 7f3342f..fccbaac 100644
--- a/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-bjDtDTaP.mjs
+++ b/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-bjDtDTaP.mjs
@@ -20,7 +20,7 @@ const useTextareaResize = (input, {
     method: "parseInt",
     nanToZero: true
   });
-  const computedMinRows = computed(() => Math.max(rowsNumber.value || 2, 2));
+  const computedMinRows = computed(() => Math.max(rowsNumber.value || 1, 1));
   const computedMaxRows = computed(() => Math.max(computedMinRows.value, maxRowsNumber.value || 0));
   const computedRows = computed(
     () => computedMinRows.value === computedMaxRows.value ? computedMinRows.value : null
@@ -44,7 +44,7 @@ const useTextareaResize = (input, {
     height.value = oldHeight;
     await nextTick();
     if (!input.value) return;
-    const contentRows = Math.max((scrollHeight - padding) / lineHeight, 2);
+    const contentRows = Math.max((scrollHeight - padding) / lineHeight, 1);
     const rows2 = Math.min(Math.max(contentRows, computedMinRows.value), computedMaxRows.value);
     const newHeight = Math.max(Math.ceil(rows2 * lineHeight + offset), minHeight);
     if (toValue(noAutoShrink) && (Number.parseFloat(oldHeight.toString()) || 0) > newHeight) {
@@ -69,7 +69,7 @@ const _sfc_main = /* @__PURE__ */ defineComponent({
   __name: "BFormTextarea",
   props: /* @__PURE__ */ mergeModels({
     noResize: { type: Boolean, default: false },
-    rows: { default: 2 },
+    rows: { default: 1 },
     wrap: { default: "soft" },
     noAutoShrink: { type: Boolean, default: false },
     maxRows: { default: void 0 },
@@ -157,7 +157,7 @@ const _sfc_main = /* @__PURE__ */ defineComponent({
         readonly: unref(props).readonly || unref(props).plaintext,
         "aria-required": unref(props).required || void 0,
         "aria-invalid": unref(computedAriaInvalid),
-        rows: unref(computedRows) || 2,
+        rows: unref(computedRows) || 1,
         style: normalizeStyle(computedStyles.value),
         wrap: unref(props).wrap || void 0,
         onInput: _cache[0] || (_cache[0] = (e) => {
diff --git a/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-pJxtI4Z7.js b/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-pJxtI4Z7.js
index 847dece..95862c3 100644
--- a/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-pJxtI4Z7.js
+++ b/node_modules/bootstrap-vue-next/dist/BFormTextarea.vue_vue_type_script_setup_true_lang-pJxtI4Z7.js
@@ -21,7 +21,7 @@ const useTextareaResize = (input, {
     method: "parseInt",
     nanToZero: true
   });
-  const computedMinRows = vue.computed(() => Math.max(rowsNumber.value || 2, 2));
+  const computedMinRows = vue.computed(() => Math.max(rowsNumber.value || 1, 1));
   const computedMaxRows = vue.computed(() => Math.max(computedMinRows.value, maxRowsNumber.value || 0));
   const computedRows = vue.computed(
     () => computedMinRows.value === computedMaxRows.value ? computedMinRows.value : null
@@ -45,7 +45,7 @@ const useTextareaResize = (input, {
     height.value = oldHeight;
     await vue.nextTick();
     if (!input.value) return;
-    const contentRows = Math.max((scrollHeight - padding) / lineHeight, 2);
+    const contentRows = Math.max((scrollHeight - padding) / lineHeight, 1);
     const rows2 = Math.min(Math.max(contentRows, computedMinRows.value), computedMaxRows.value);
     const newHeight = Math.max(Math.ceil(rows2 * lineHeight + offset), minHeight);
     if (vue.toValue(noAutoShrink) && (Number.parseFloat(oldHeight.toString()) || 0) > newHeight) {
@@ -70,7 +70,7 @@ const _sfc_main = /* @__PURE__ */ vue.defineComponent({
   __name: "BFormTextarea",
   props: /* @__PURE__ */ vue.mergeModels({
     noResize: { type: Boolean, default: false },
-    rows: { default: 2 },
+    rows: { default: 1 },
     wrap: { default: "soft" },
     noAutoShrink: { type: Boolean, default: false },
     maxRows: { default: void 0 },
@@ -158,7 +158,7 @@ const _sfc_main = /* @__PURE__ */ vue.defineComponent({
         readonly: vue.unref(props).readonly || vue.unref(props).plaintext,
         "aria-required": vue.unref(props).required || void 0,
         "aria-invalid": vue.unref(computedAriaInvalid),
-        rows: vue.unref(computedRows) || 2,
+        rows: vue.unref(computedRows) || 1,
         style: vue.normalizeStyle(computedStyles.value),
         wrap: vue.unref(props).wrap || void 0,
         onInput: _cache[0] || (_cache[0] = (e) => {
